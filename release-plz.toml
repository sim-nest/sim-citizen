# release-plz tracks synchronized versions for the publishable crates in this
# workspace. Dev harnesses carry publish=false in their own Cargo.toml and stay
# outside the publish set.
[workspace]
# Keep crate versions synchronized across this repository.
dependencies_update = true
git_release_enable = true
# release-plz publishes crates in dependency order.
